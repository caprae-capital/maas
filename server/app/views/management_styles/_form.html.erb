<%= form_with(model: management_style) do |form| %>
  <% if management_style.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(management_style.errors.count, "error") %> prohibited this management_style from being saved:</h2>

      <ul>
        <% management_style.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
