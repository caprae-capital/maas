<%= form_with(model: business_model) do |form| %>
  <% if business_model.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(business_model.errors.count, "error") %> prohibited this business_model from being saved:</h2>

      <ul>
        <% business_model.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
