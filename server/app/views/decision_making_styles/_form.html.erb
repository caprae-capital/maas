<%= form_with(model: decision_making_style) do |form| %>
  <% if decision_making_style.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(decision_making_style.errors.count, "error") %> prohibited this decision_making_style from being saved:</h2>

      <ul>
        <% decision_making_style.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
