<%= form_with(model: innovation_style) do |form| %>
  <% if innovation_style.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(innovation_style.errors.count, "error") %> prohibited this innovation_style from being saved:</h2>

      <ul>
        <% innovation_style.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
