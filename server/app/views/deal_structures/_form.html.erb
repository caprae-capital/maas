<%= form_with(model: deal_structure) do |form| %>
  <% if deal_structure.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(deal_structure.errors.count, "error") %> prohibited this deal_structure from being saved:</h2>

      <ul>
        <% deal_structure.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
