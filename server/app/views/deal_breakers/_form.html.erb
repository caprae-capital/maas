<%= form_with(model: deal_breaker) do |form| %>
  <% if deal_breaker.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(deal_breaker.errors.count, "error") %> prohibited this deal_breaker from being saved:</h2>

      <ul>
        <% deal_breaker.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
