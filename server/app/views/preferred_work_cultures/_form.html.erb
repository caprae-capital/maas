<%= form_with(model: preferred_work_culture) do |form| %>
  <% if preferred_work_culture.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(preferred_work_culture.errors.count, "error") %> prohibited this preferred_work_culture from being saved:</h2>

      <ul>
        <% preferred_work_culture.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
